# Runtime: PER/UPER Codec Engine
# Subdirectories correspond to encoding schemes; 'per' is a future placeholder.

add_subdirectory(core)
add_subdirectory(uper)
# add_subdirectory(per)   # uncomment when PER support is added
